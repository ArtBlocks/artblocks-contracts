// expected revert messages
export const revertMessages = {
  onlyDeployerAbandon: "only deployer may abandon",
  factoryAbandoned: "factory is abandoned",
  missingRequiredSplit: "splits[0] must be required split",
  invalidSplitBPS: "only reqd split BPS <= 10_000",
};
